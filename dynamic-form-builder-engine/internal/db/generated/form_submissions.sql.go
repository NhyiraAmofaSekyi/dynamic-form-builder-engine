// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: form_submissions.sql

package generated

import (
	"context"
	"encoding/json"

	"github.com/google/uuid"
)

const countSubmissionsByForm = `-- name: CountSubmissionsByForm :one
SELECT COUNT(*)::bigint FROM form_submissions
WHERE form_id = $1
`

func (q *Queries) CountSubmissionsByForm(ctx context.Context, formID uuid.UUID) (int64, error) {
	row := q.db.QueryRow(ctx, countSubmissionsByForm, formID)
	var column_1 int64
	err := row.Scan(&column_1)
	return column_1, err
}

const countSubmissionsByVersion = `-- name: CountSubmissionsByVersion :one
SELECT COUNT(*)::bigint FROM form_submissions
WHERE form_id = $1 AND form_version_id = $2
`

type CountSubmissionsByVersionParams struct {
	FormID        uuid.UUID `json:"form_id"`
	FormVersionID uuid.UUID `json:"form_version_id"`
}

func (q *Queries) CountSubmissionsByVersion(ctx context.Context, arg CountSubmissionsByVersionParams) (int64, error) {
	row := q.db.QueryRow(ctx, countSubmissionsByVersion, arg.FormID, arg.FormVersionID)
	var column_1 int64
	err := row.Scan(&column_1)
	return column_1, err
}

const getSubmissionByID = `-- name: GetSubmissionByID :one
SELECT id, form_id, form_version_id, submission_data, status, submitted_by, submitted_at, created_at, updated_at FROM form_submissions
WHERE id = $1
`

func (q *Queries) GetSubmissionByID(ctx context.Context, id uuid.UUID) (FormSubmission, error) {
	row := q.db.QueryRow(ctx, getSubmissionByID, id)
	var i FormSubmission
	err := row.Scan(
		&i.ID,
		&i.FormID,
		&i.FormVersionID,
		&i.SubmissionData,
		&i.Status,
		&i.SubmittedBy,
		&i.SubmittedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const insertSubmission = `-- name: InsertSubmission :one
INSERT INTO form_submissions (
    form_id, form_version_id, submission_data, status, submitted_by
)
VALUES ($1, $2, $3, $4, $5)
    RETURNING id, form_id, form_version_id, submission_data, status, submitted_by, submitted_at, created_at, updated_at
`

type InsertSubmissionParams struct {
	FormID         uuid.UUID        `json:"form_id"`
	FormVersionID  uuid.UUID        `json:"form_version_id"`
	SubmissionData json.RawMessage  `json:"submission_data"`
	Status         SubmissionStatus `json:"status"`
	SubmittedBy    *uuid.UUID       `json:"submitted_by"`
}

// submitted_by is nullable: NULL = anonymous. The handler passes NULL for
// submissions submissions (no authenticated user). The submission is pinned to the
// exact form_version_id that validated it — the historical-integrity guarantee.
func (q *Queries) InsertSubmission(ctx context.Context, arg InsertSubmissionParams) (FormSubmission, error) {
	row := q.db.QueryRow(ctx, insertSubmission,
		arg.FormID,
		arg.FormVersionID,
		arg.SubmissionData,
		arg.Status,
		arg.SubmittedBy,
	)
	var i FormSubmission
	err := row.Scan(
		&i.ID,
		&i.FormID,
		&i.FormVersionID,
		&i.SubmissionData,
		&i.Status,
		&i.SubmittedBy,
		&i.SubmittedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listSubmissionsByForm = `-- name: ListSubmissionsByForm :many
SELECT id, form_id, form_version_id, submission_data, status, submitted_by, submitted_at, created_at, updated_at FROM form_submissions
WHERE form_id = $1
ORDER BY submitted_at DESC NULLS LAST, created_at DESC
    LIMIT $2 OFFSET $3
`

type ListSubmissionsByFormParams struct {
	FormID uuid.UUID `json:"form_id"`
	Limit  int32     `json:"limit"`
	Offset int32     `json:"offset"`
}

// all submissions for a form, across every version, newest first
func (q *Queries) ListSubmissionsByForm(ctx context.Context, arg ListSubmissionsByFormParams) ([]FormSubmission, error) {
	rows, err := q.db.Query(ctx, listSubmissionsByForm, arg.FormID, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []FormSubmission
	for rows.Next() {
		var i FormSubmission
		if err := rows.Scan(
			&i.ID,
			&i.FormID,
			&i.FormVersionID,
			&i.SubmissionData,
			&i.Status,
			&i.SubmittedBy,
			&i.SubmittedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listSubmissionsByVersion = `-- name: ListSubmissionsByVersion :many
SELECT id, form_id, form_version_id, submission_data, status, submitted_by, submitted_at, created_at, updated_at FROM form_submissions
WHERE form_id = $1 AND form_version_id = $2
ORDER BY submitted_at DESC NULLS LAST, created_at DESC
    LIMIT $3 OFFSET $4
`

type ListSubmissionsByVersionParams struct {
	FormID        uuid.UUID `json:"form_id"`
	FormVersionID uuid.UUID `json:"form_version_id"`
	Limit         int32     `json:"limit"`
	Offset        int32     `json:"offset"`
}

// submissions for ONE specific version of a form
func (q *Queries) ListSubmissionsByVersion(ctx context.Context, arg ListSubmissionsByVersionParams) ([]FormSubmission, error) {
	rows, err := q.db.Query(ctx, listSubmissionsByVersion,
		arg.FormID,
		arg.FormVersionID,
		arg.Limit,
		arg.Offset,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []FormSubmission
	for rows.Next() {
		var i FormSubmission
		if err := rows.Scan(
			&i.ID,
			&i.FormID,
			&i.FormVersionID,
			&i.SubmissionData,
			&i.Status,
			&i.SubmittedBy,
			&i.SubmittedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
